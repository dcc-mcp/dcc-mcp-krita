[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "dcc-mcp-krita"
version = "0.1.0"
description = "Krita adapter for the DCC Model Context Protocol"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "loonghao", email = "hal.long@outlook.com" }]
keywords = ["krita", "image-editing", "mcp", "dcc", "automation"]
dependencies = ["dcc-mcp-core>=0.19.38,<1.0.0"]

[project.optional-dependencies]
dev = ["build>=1.2", "pytest>=8", "ruff>=0.8", "twine>=6"]

[project.urls]
Homepage = "https://github.com/dcc-mcp/dcc-mcp-krita"
Repository = "https://github.com/dcc-mcp/dcc-mcp-krita"
Issues = "https://github.com/dcc-mcp/dcc-mcp-krita/issues"

[project.scripts]
dcc-mcp-krita = "dcc_mcp_krita.server:main"
dcc-mcp-krita-install = "dcc_mcp_krita.install:main"

[project.entry-points."dcc_mcp.adapters"]
krita = "dcc_mcp_krita:KritaMcpServer"

[tool.hatch.build.targets.wheel]
packages = ["src/dcc_mcp_krita"]
artifacts = ["src/dcc_mcp_krita/skills/**"]

[tool.hatch.build.targets.wheel.force-include]
"bridge/krita-plugin/dcc_mcp_krita.py" = "dcc_mcp_krita/krita_plugin/dcc_mcp_krita.py"

[tool.hatch.build.targets.sdist]
include = ["src/dcc_mcp_krita", "bridge", "tests", "README.md", "LICENSE", "CHANGELOG.md", "tools"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
